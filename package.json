{
	"name": "frb-socs-ics-calendar-parser",
	"version": "1.0.2",
	"devDependencies": {
		"auto-changelog": "*"
	},
	"scripts": {
		"version": "auto-changelog -p --template changelog-templates/template.hbs && git add CHANGELOG.md && git commit -m 'Update Changelog'"
	}
}
